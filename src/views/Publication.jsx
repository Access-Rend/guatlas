import CSVTable from "../components/CSVTable"

const Publication = () => {
    return (<div>
        <CSVTable onClick={() => {}} url={'/DB/5.Publication/publication.csv'} />
    </div>)
}

export default Publication